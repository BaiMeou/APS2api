# Vertex AI Proxy 部署指南

把 **OpenAI 兼容 API** 翻译成对 Google 匿名端点的调用，免 API Key 使用 Gemini 系列模型。
单文件二进制，无任何运行时依赖（不需要 Python / Node / Docker），下载即跑。

本指南覆盖 **Windows / Linux / Android(Termux)** 三种部署方式，以及代理、开机自启、更新等运维事项。

---

## 目录

- [一、通用准备](#一通用准备)
- [二、Windows 部署](#二windows-部署)
- [三、Linux 部署](#三linux-部署)
- [四、Android（Termux）部署](#四androidtermux-部署)
- [五、Docker 部署](#五docker-部署)
- [六、配置说明](#六配置说明)
- [七、代理与节点（应对 IP 限制 / 网络封锁）](#七代理与节点应对-ip-限制--网络封锁)
- [八、运维事项](#八运维事项)
- [九、常见问题](#九常见问题)

---

## 一、通用准备

不管哪个平台，逻辑都一样：**一个可执行文件 + 一个 `config` 目录**。目录结构如下：

```
vertex-proxy(.exe)          ← 主程序
config/
  ├── config.json           ← 运行配置（首次启动自动生成）
  ├── api_keys.txt          ← 访问密钥（必须自己填，否则所有请求 401）
  └── models.json           ← 模型清单（已内置，一般不用改）
```

程序按 **可执行文件同级的 `config/` 目录** 读取配置，所以解压后保持上面的相对结构即可，从任意目录启动都能找到。

### 两件必做的事

1. **填一个访问密钥**：把 `config/api_keys.example.txt` 复制为 `config/api_keys.txt`，里面每行一个密钥，格式 `名称:密钥:备注`，密钥须以 `sk-` 开头。例如：

   ```
   mykey:sk-abc123456789:我的密钥
   ```

   客户端（OpenAI SDK / 酒馆 / Cherry Studio 等）用这个 `sk-...` 作为 API Key，Base URL 填 `http://<服务器地址>:2156/v1`。

2. **记下管理员密码**：首次启动时，如果没设密码，程序会**自动生成一个并打印在日志里**（只打印这一次），形如：

   ```
   ============================================================
   [*] 首次启动，已自动生成管理员密码：
       密码: x3Kp9_aQ2mZv
       访问: http://<host>:<port>/admin
   ============================================================
   ```

   用它登录 `http://<服务器地址>:2156/admin/` 管理面板（可改密钥、模型、代理、设置）。密码也会写进 `config/config.json`，忘了可以去那里看。

> 各平台下面的「启动脚本」会自动完成第 1 步的复制（缺文件时从 example 拷一份），你只要把密钥填进去即可。

---

## 二、Windows 部署

适用：`vertex-proxy-windows-amd64.zip`

1. 解压到任意目录，例如 `D:\vertex-proxy\`。
2. 双击 **`启动.bat`**（推荐）。它会：
   - 缺 `config.json` / `api_keys.txt` 时从示例自动复制一份；
   - 启动程序，并**保持窗口不关闭**，方便你看到首次的管理员密码。
3. 第一次启动后，按窗口提示编辑 `config\api_keys.txt`，填上你的 `sk-` 密钥，然后关掉窗口重新双击 `启动.bat`。
4. 浏览器打开 `http://127.0.0.1:2156/admin/`，用日志里的密码登录。

也可以手动在 PowerShell / CMD 里运行：

```bat
cd /d D:\vertex-proxy
vertex-proxy.exe
```

**后台 / 开机自启**：Windows 没有内置的简单守护方式，推荐两种：
- 用 [NSSM](https://nssm.cc/)：`nssm install VertexProxy D:\vertex-proxy\vertex-proxy.exe`，把它注册成系统服务，开机自动拉起。
- 或「任务计划程序」新建一个「登录时运行」的任务，程序填 `vertex-proxy.exe`，起始位置填解压目录。

**防火墙**：只在本机用无需操作；要让局域网内其它设备访问，放行 TCP `2156` 入站。

---

## 三、Linux 部署

适用：`vertex-proxy-linux-amd64.zip`（x86_64 服务器/VPS）

```bash
# 1. 解压
unzip vertex-proxy-linux-amd64.zip -d vertex-proxy && cd vertex-proxy

# 2. 赋予执行权限
chmod +x vertex-proxy

# 3. 用启动脚本跑（自动初始化配置）
./start.sh
```

首次启动看日志里的管理员密码，然后编辑 `config/api_keys.txt` 填上你的 `sk-` 密钥，再重启。

### 作为 systemd 服务常驻（推荐）

压缩包内附 `vertex-proxy.service` 模板。安装步骤：

```bash
# 假设解压在 /opt/vertex-proxy
sudo mv vertex-proxy /opt/vertex-proxy && cd /opt/vertex-proxy   # 如尚未就位
sudo cp vertex-proxy.service /etc/systemd/system/
# 按需修改 service 里的 WorkingDirectory / User
sudo systemctl daemon-reload
sudo systemctl enable --now vertex-proxy
sudo systemctl status vertex-proxy      # 查看状态
journalctl -u vertex-proxy -f           # 看实时日志（含首次管理员密码）
```

### 简单后台（不想用 systemd）

```bash
nohup ./vertex-proxy > vproxy.log 2>&1 &
tail -f vproxy.log     # 看密码
```

### 反向代理（可选）

如需 HTTPS / 域名，在前面挂 Nginx：

```nginx
location / {
    proxy_pass http://127.0.0.1:2156;
    proxy_http_version 1.1;
    proxy_set_header Connection "";
    proxy_buffering off;        # SSE 流式必须关缓冲
    proxy_read_timeout 3600s;   # 长流式 / 大文件别被切断
}
```

---

## 四、Android（Termux）部署

适用：`vertex-proxy-android-arm64.zip`（绝大多数手机是 ARM64）

1. 从 **[F-Droid](https://f-droid.org/packages/com.termux/)** 安装 Termux（不要用 Play 商店那个旧版）。
2. 在 Termux 里：

   ```bash
   pkg update -y && pkg install -y unzip

   # 把下载好的 zip 放到 Termux 可访问的目录后解压
   # （手机下载目录通常是 ~/storage/downloads，需先 termux-setup-storage 授权）
   unzip vertex-proxy-android-arm64.zip -d vertex-proxy
   cd vertex-proxy
   chmod +x vertex-proxy

   sh start.sh
   ```

   > Termux 没有 `/bin/sh`，所以用 `sh start.sh` 启动（而不是 `./start.sh`）。

3. 首次启动看密码，编辑 `config/api_keys.txt`（`nano config/api_keys.txt`）填密钥，重启。
4. 本机访问 `http://127.0.0.1:2156/v1`；同一 WiFi 下其它设备访问 `http://<手机局域网IP>:2156/v1`（`ifconfig` 看 IP）。

**让它别被系统杀后台**：

```bash
termux-wake-lock      # 申请唤醒锁，降低被系统回收概率
```

并在系统设置里给 Termux「忽略电池优化 / 允许后台运行」。

**开机自启**（可选）：装 **Termux:Boot** 插件，把启动命令写进 `~/.termux/boot/start-vproxy.sh`：

```bash
#!/data/data/com.termux/files/usr/bin/sh
termux-wake-lock
cd ~/vertex-proxy && ./vertex-proxy
```

> ARM64 二进制同样适用于树莓派、ARM 云服务器等 Linux ARM 设备，用法同 Linux 部署。

---

## 五、Docker 部署

如果你熟悉 Docker，仓库根目录带 `Dockerfile` 和 `docker-compose.yml`：

```bash
docker compose up -d
docker compose logs -f      # 看首次管理员密码
```

配置通过挂载卷 `./config` 持久化，首次启动容器会自动初始化默认配置。

---

## 六、配置说明

`config/config.json`（不存在时用内置默认值；改完发 `SIGHUP` 或重启生效）。

### 常用项

| 字段 | 默认 | 说明 |
|------|------|------|
| `port_api` | `2156` | 监听端口 |
| `admin_password` | 自动生成 | 管理面板密码（留空首启自动生成并写回） |
| `max_retries` | `2` | 上游请求失败的重试次数 |
| `proxy_url` | `""` | 单一出站代理（`socks5://` 或 `http://`） |
| `anti429_enabled` | `false` | 是否插入抗 429 提示以缓解上游限流 |
| `force_no_stream` | `false` | 强制所有请求走非流式 |

最小可用的 `config.json` 示例：

```json
{
  "port_api": 2156,
  "max_retries": 10,
  "admin_password": "",
  "proxy_url": ""
}
```

### 进阶项（一般不用动）

| 字段 | 默认 | 说明 |
|------|------|------|
| `max_n` | `8` | 单请求 `n` 多候选的上限 |
| `max_request_mb` | `0` | 入站请求体上限（MB），`0` = 不限 |
| `drop_max_tokens` | `false` | 丢弃客户端传入的 `max_tokens` |
| `anti_tracking` | `true` | 注入抗追踪扰动 |
| `active_node_uri` / `parallel_pool_*` | 见 [第七节](#七代理与节点应对-ip-限制--网络封锁) | 代理节点池 |

### 模型清单

`config/models.json` 控制 `/v1/models` 返回的模型与别名：

```json
{
  "models": ["gemini-3.5-flash", "gemini-2.5-pro", "..."],
  "alias_map": { "gpt-4o": "gemini-3.5-flash" }
}
```

`alias_map` 可把客户端写死的模型名（如 `gpt-4o`）映射到实际 Gemini 模型，方便不能改模型名的客户端直接用。也可以在管理面板「模型」页可视化编辑。

**假流式**：模型名加前缀 `假流式-` 或 `fake-`（如 `fake-gemini-2.5-pro`）会先完整生成、再切片伪装成流式输出，适合不支持非流式的客户端。

---

## 七、代理与节点（应对 IP 限制 / 网络封锁）

上游匿名端点对来源 IP 较敏感，住宅/数据中心 IP 信誉差时可能频繁失败。三种应对方式，按需选一：

1. **单一出站代理**：`config.json` 里设 `proxy_url`，所有上游请求都走它。
   ```json
   { "proxy_url": "socks5://127.0.0.1:1080" }
   ```

2. **锁定订阅节点**：在管理面板「代理与节点」页填入订阅/节点 URI（支持 `vless` / `vmess` / `ss` / `trojan`）。已内嵌 sing-box，无需另开本地代理端口。

3. **并发竞速池**：从一组节点里并发发起、谁先回谁赢，提升成功率与速度。在面板里开启「并发池」并填多个节点即可。

> 在国内/受限网络的设备（含安卓手机）上自部署时，第 2、3 种方式通常最稳。

---

## 八、运维事项

- **健康检查**：`GET /health` 返回 `{"status":"healthy",...}`，可用于监控/探活。
- **热重载配置**：改完 `config.json` / `api_keys.txt` / `models.json` 后，无需重启——
  - Linux/Termux：`kill -HUP <pid>`（或 `systemctl reload` 若已配），立即清缓存、下次请求即生效；
  - 不发信号也行，配置有 60 秒缓存，最多 1 分钟自动生效。
- **优雅关闭**：收到 `SIGINT`/`SIGTERM`（Ctrl-C / `systemctl stop`）会先排干在途请求（最长 25 秒）再退出，不会粗暴切断正在进行的流式响应。
- **更新版本**：停服 → 用新版二进制替换旧的 → 重启。`config/` 目录不受影响，配置/密钥都保留。
- **日志**：直接打到标准输出。systemd 用 `journalctl -u vertex-proxy`，nohup 看你重定向的文件，Docker 用 `docker compose logs`。

---

## 九、常见问题

**Q：所有请求都返回 401 / "missing API key"？**
A：没填 `config/api_keys.txt`，或客户端用的 key 不在里面。确认文件里有一行 `名称:sk-xxx:备注`，且客户端 API Key 就是那个 `sk-xxx`。

**Q：管理面板登录提示密码错误？**
A：用首次启动日志里打印的密码，或看 `config/config.json` 的 `admin_password` 字段。改密码在面板「设置」页。

**Q：上游一直失败 / 429 / 认证失败？**
A：多半是出口 IP 信誉差。参考[第七节](#七代理与节点应对-ip-限制--网络封锁)挂代理或节点池；也可调大 `max_retries`。

**Q：流式响应中文/emoji 变乱码？**
A：v1.0.1 起已修复（按字符切分），请用最新版。

**Q：局域网其它设备连不上？**
A：程序默认监听 `0.0.0.0`，检查防火墙是否放行 `2156` 端口，以及用的是设备的局域网 IP 而非 `127.0.0.1`。

**Q：能换端口吗？**
A：改 `config.json` 的 `port_api`，重启即可。

---

许可证：MIT
