# ── Vertex AI Proxy 环境变量 ──────────────────────────────────────────────────

# 出站代理（可选）
# 填写后所有到 Vertex AI 的请求都会经过此代理
# 支持 HTTP / SOCKS5
PROXY_URL=
# PROXY_URL=socks5://127.0.0.1:7890
# PROXY_URL=http://user:pass@proxy.example.com:3128

# 映射到宿主机的端口（默认 2156）
PORT=2156
